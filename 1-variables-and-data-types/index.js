let array = ['texto', 10, true, 'QA']

array.push(33) // Para incluir uma nova posição.

array.pop() // Para retirar o último item.

array[0] // Mostra conteúdo refente a posição selecionada.

let objeto = {marca: 'BMW', modelo: 'X6', cor: 'Preto Fosco', km: 0, status: true}

objeto.marca // Retorna o conteúdo que foi selecionado.

objeto.donos = 0 // Adicionar uma informação.

objeto.cor = 'preto'
